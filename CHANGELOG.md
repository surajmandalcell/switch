# Changelog

## Unreleased

- Replaced the Electron gateway entry point with a native macOS Swift package.
- Added shared `AIManagerCore`, SwiftUI `AIManager`, and terminal `ai-manager`
  products.
- Added a canonical `/private/tmp/ai-manager-build` cache and native app packaging metadata.
- Added a custom borderless account interface with macOS colors and keyboard navigation.
- Retired provider routing, local HTTP server, website, and cross-platform
  desktop packaging from the active product.
